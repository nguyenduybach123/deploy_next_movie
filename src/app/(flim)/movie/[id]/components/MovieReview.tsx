import Rating from '@/app/(flim)/components/Rating';
import React from 'react';

const MovieReview = () => {
    return (
        <div>
            <Rating />
        </div>
    );
};

export default MovieReview;
